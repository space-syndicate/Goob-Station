uniform lowp float glowEnergy; // how strong the emissive should be

void fragment() {
    highp vec4 color = zTexture(UV);

    lowp vec3 lightSample = texture2D(lightMap, Pos).rgb;
    COLOR = texColor * vec4(lightSample, 1.0) + glowEnergy;
}
